import PostForm from "../shared/PostForm";

export default function PostPage() {
  return (
    <div style={{ padding: 24 }}>
      <h2>Create a Post (with image)</h2>
      <PostForm />
    </div>
  );
}
